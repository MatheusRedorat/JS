const pessoa = {
    nome: "João",
    idade: 30,
    cidade: "São Paulo"
};

// Apresentando as informações da pessoa em uma string
console.log(`Nome: ${pessoa.nome}, Idade: ${pessoa.idade}, Cidade: ${pessoa.cidade}`);
